import { View, Text } from 'react-native';

function VideohScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>Video Screen</Text>
      </View>
    );
  }

export default VideohScreen;